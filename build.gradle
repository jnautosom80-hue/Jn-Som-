// Configuração moderna de plugins para evitar o erro de DependencyHandler
plugins {
    id 'com.android.application' version '8.1.0' apply false
}

tasks.register('clean', Delete) {
    delete rootProject.buildDir
}
